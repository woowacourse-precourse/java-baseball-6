package baseball;

public class Calculate {
    public int divideNumber(int num, int digit) {
        int number = num / digit;
        return number;
    }

    public int multiplyNumber(int num, int digit) {
        int number = num * digit;
        return number;
    }
}
