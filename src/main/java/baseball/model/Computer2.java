package baseball.model;

import java.util.Random;

public class Computer2 {
    /*
    private int ballnum;
    private int index;

    public Computer2(){}
        Random rd   = new Random();

        //번호 섞기
        public void mix(int[] rnum){
            //번호 랜덤 생성
            index = rd.nextInt(9);

            //서로 다른 수이기 때문에 중복이 나오지 않도록
            if (rnum[index] == 0){
                while (rnum[index] == 0){
                    index = rd.nextInt(9);
                }
            }
            //고른 숫자를 ballnum 변수에 저장
            ballnum = rnum[index];
            //이미 나온숫자는 0으로 초기화
            rnum[index] = 0;
        }
    public int outNum(){
            return ballnum;
    }
    */

}
